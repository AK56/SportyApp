<%= form_with(model: sport, local: true) do |form| %>
  <% if sport.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sport.errors.count, "error") %> prohibited this sport from being saved:</h2>

      <ul>
      <% sport.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div id="icon_field" class="field">
    <% if sport.icon.attached? %>
      <%= image_tag sport.icon, size: "100" %>
    <% end %>
    <div class="clear"></div>
    <%= form.file_field :icon %>
  </div>
  <div class="clear"></div>
  <div id="name_field" class="field">
    <%= form.label 'Name: '%>
    <%= form.text_field :name %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
